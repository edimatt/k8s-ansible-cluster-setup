---
  - name: Apply sysctl settings
    ansible.builtin.command: sysctl --system
    changed_when: true

  - name: Restart chrony
    ansible.builtin.systemd:
      name: chrony
      enabled: true
      state: restarted
